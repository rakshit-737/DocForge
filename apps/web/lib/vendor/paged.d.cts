/* Type declaration for the vendored Paged.js UMD (paged.cjs, generated by
   scripts/sync-assets.mjs and gitignored). Committed so a fresh checkout
   typechecks before the asset sync has run. */
declare const paged: typeof import("pagedjs");
export = paged;
