# Golden corpus documents are byte-exact test input — never rewrite their line endings.
qa/golden/corpus/*.md text eol=lf
qa/torture.md text eol=lf
# The frozen classic engine drives byte-parity tests; its template literals make
# line endings part of the strings it emits.
packages/engine/test/fixtures/classic-engine.js text eol=lf
