# Appendix A — feature-parity audit of the web studio (Phase 2/3 gate record)

Audited 2026-08-31 against `apps/web` as committed, running the production build
(`next build` → `next start -p 3210`) under headless Chromium (`qa/_browser.mjs`,
fresh profiles). Every verdict cites a file or a live probe; per MASTER-PROMPT
§10.5 nothing below is claimed without one. The single-file edition
(`dist/DocForge.html`, built from `src/`) is the classic reference.

**Verdict key** — DONE: the web studio has it, with evidence. PARTIAL: part
exists, part doesn't; both halves named. CLASSIC-ONLY: the single-file edition
has it, the web studio does not yet. N/A: not applicable.

Probe note: mid-audit, a `next start` that predated a `.next` rebuild served
stale HTML pointing at a dead CSS chunk, which broke the shell's fixed-viewport
layout in several runs; every probe that raced that window (outline jump, the
height chain) was re-run against a freshly started server and the re-run is
what's recorded here.

---

## Row 1 — Live paginated preview: A4/Letter, 3 margin presets, zoom (ctrl+wheel, fit, %), folio readout, outline navigator — **PARTIAL → DONE (2026-09-03)**

| Item | Verdict | Evidence |
| --- | --- | --- |
| A4 / Letter | DONE | `settings-drawer.tsx` PAGE_OPTIONS → `Engine.PAGES`; A4 composed live (probe A) |
| 3 margin presets | DONE | MARGIN_OPTIONS normal/narrow/wide → `@page margin` in `packages/engine/src/themes.ts` |
| Zoom: fit + % buttons | DONE | Live: footer cluster 100% → +→ 120% → Fit → 114% (fit recalc); `preview-controller.ts` setZoom/applyZoom, fit cap 1.35 |
| Zoom: ctrl+wheel | DONE (`e9a8310`) | Ported onto `[data-preview-scroll]` (`preview-deck.tsx`): ctrl+wheel steps 0.1 within 0.25–2, clicking the percentage still snaps back to fit. Audit-day verdict was CLASSIC-ONLY — classic `src/js/main.js:2350` binds it, the web shell then bound nothing |
| Folio readout | DONE | Live: wire ticker reads "p. 3 · 3 pages" and follows scroll (outline jump moved it p. 1 → p. 3); `updatePageIndicator()` |
| Outline navigator | DONE | Live: panel lists One/Two/Three (h1–h3), click scrolled the stone to page 3 (scrollTop 0 → 2425); `outline-panel.tsx` roving list |

## Row 2 — Live-edit on pages: caret-preserving recompose, view re-anchoring, read-only generated furniture, one undo history — **DONE**

| Item | Verdict | Evidence |
| --- | --- | --- |
| Editing surface + splice to source | DONE | Live: typed " Typed on the galley." into a rendered paragraph; after the 1300 ms idle it appeared verbatim in the CodeMirror source (`lib/live-edit.ts` splice → `spliceSource`) |
| Caret-preserving recompose / re-anchoring | DONE | `preview-controller.ts` drives the classic points (flush → captureView → compose → swap → arm → restoreView); typing continued at the caret across the recompose in the probe; stage record (02-studio.md) measured 0 px anchor drift |
| Read-only generated furniture | DONE | Live: the `[toc]` block's nearest contenteditable host is `contenteditable="false"`; unmappable edits revert with a toast (`revert` hook, `preview-deck.tsx`) |
| One undo history across panes | DONE | Live: Ctrl+Z with focus on the manuscript removed the page-typed text from the source (`docHistory` + the deck keydown router) |

## Row 3 — Dialect complete (Appendix B), parser tests per construct — **DONE**

`packages/engine/test/`: 127 tests across inline-marks, span-attributes,
headings-toc-xref, citations-footnotes, math-code, blocks-figures, preprocess,
render, themes, typography, parity — one file per construct family. The web
studio renders through this same engine (`lib/bootstrap.ts` assigns globals,
then imports `@docforge/engine`); `qa/golden/corpus` holds 17 torture docs.
Live spot-checks in probe A exercised headings+labels, toc/lof/lot, footnote,
citation+locator, cross-refs, table caption, figure, pagebreak, display math.

## Row 4 — Themes ×4, accent colours, page borders 7×3×ink/accent, cover page, page-numbering scheme — **DONE**

| Item | Verdict | Evidence |
| --- | --- | --- |
| Themes ×4 | DONE | THEME_OPTIONS modern/executive/academic/minimal; live: executive gave serif `--font-head` ("DocForge Serif, Cambria…"), minimal re-composed |
| Accent colours | DONE | 8 swatches + custom well (`settings-drawer.tsx` ACCENTS); accent-follows-theme in the store |
| Borders 7 styles × 3 weights × ink/accent | DONE | All 7×3×2 generated by one BORDERS map (`themes.ts:493`); live: double/bold/accent rendered `8.03px double rgb(27,51,84)` on body pages; thickthin/fine/ink rendered solid + inset ring shadow |
| Cover page | DONE | Live: `pagedjs_cover_page` present when toggled, exempt from the border (`::after` content: none) |
| Numbering scheme | DONE | Live per-page `--df-foot`: cover `""` · front pages `"i" "ii" "iii"` (roman) · body `"Page 1 of 4"…` counting body pages only (`PageNumbering` handler) |

## Row 5 — TOC/LOF/LOT with dotted leaders + real page numbers; footnotes at page foot; citations both styles + locators; cross-refs; table captions; header-row repeat — **DONE**

| Item | Verdict | Evidence |
| --- | --- | --- |
| TOC dotted leaders + real folios | DONE | Live: every `.toc a` carries a `.dots` span computing `border-bottom: dotted` and a `--df-tocnum` of the page's actual folio ("1","2","4") |
| LOF / LOT | DONE | Live: `[lof]`/`[lot]` rendered lists ("Figure 1 — The apparatus" p.3, "Table 1 — Results table" p.2) with the same leader treatment |
| Footnotes at correct page foot | DONE | Live: note text sits in the `.pagedjs_footnote_area` of the page carrying the call, at the page foot; `FootnoteFix` handler re-measures reserved strips |
| Citations both styles + locators | DONE | Live: numeric `[1]` + locator "p. 12" rendered (ieee); apa option present (CITE_OPTIONS), same engine path as the classic goldens |
| Cross-references resolve | DONE | Live: `[#tbl:res]`→"Table 1", `[#fig:one]`→"Figure 1" (NBSP-joined), `[#sec:data]`→section title |
| Table captions number | DONE | Live: "Table 1 — Results table" from `[table: … | #tbl:res]` |
| Header row repeats on page-breaks | DONE | Live: the split table's continuation carries `thead[data-repeated-header]` (`RepeatTableHeader` in renderNode) |

## Row 6 — Fonts: embedded families in preview/PDF/DOCX identically; ~190-name Word menu with installed-detection; per-selection font/size — **DONE**

| Item | Verdict | Evidence |
| --- | --- | --- |
| Embedded families everywhere | DONE | `Engine.EMBEDDED` (8 stems: Sans/Serif/Mono/Inter/Mont/Garamond/Crimson/+1) served as `/fonts/*.ttf` + `fonts.css` for preview and print; exports fetch the same bytes into `__FONT_DATA__` (`bootstrap.ts loadFontData`) — the exported DOCX carried 4 `.odttf` embeds for the faces in use |
| ~190-name Word menu + installed-detection | DONE | `WORD_CATALOG` = 191 names in 5 specimen groups; canvas-measured `fontInstalled` appends " · not on this device" in both the toolbar select and the settings drawer |
| Per-selection font/size | DONE | Toolbar Typeface…/Size… selects (snap-back grammar) → `setTextFont`/`setTextSize` span attributes (`editor-commands.ts`), the same wrapCmd machinery live-probed for the other marks |

## Row 7 — Word ribbon parity: underline/strike/highlight(15)/sub/sup/colour/shading/size/face/small caps/all caps/alignment/change case/clear formatting — identical in PDF, real run properties in DOCX — **PARTIAL → DONE (2026-09-03)**

| Item | Verdict | Evidence |
| --- | --- | --- |
| Toolbar marks on a source selection | DONE | All plates present (`toolbar.tsx`); live keyboard probes produced `**…**`, `++…++`, `~~…~~`, `==…==`; highlight = 15 Word inks (HL_COLORS), colour dozen + custom, shading palette + custom, change case, clear formatting, 4 alignment blocks, sub/sup |
| Small caps / all caps | DONE | Dialect span attrs `sc`/`caps` render in preview/PDF and export as real `smallCaps`/`allCaps` run properties (`docx-export.ts:260`); no toolbar plate in either edition — parity holds |
| Identical in PDF / real DOCX run props | DONE | Shared engine + `@docforge/export-docx` (the classic golden path); exported file inspected below |
| Ribbon marks on a **manuscript** selection | DONE | `LiveEdit.styleSelection` ported (`lib/live-edit.ts`, the classic wrap-and-queue verbatim) and the four run-level controls dual-road in `toolbar.tsx` (applyHl/applyColor/applyFont/applySize — manuscript first, source-pane fallback). Probed live: highlight shows `<mark data-hl>` at once, source gains `==word==` on flush, both highlight and colour survive recompose (the round-trip proof), editor selections still take the source road |

## Row 8 — DOCX export: embedded fonts, styles, cover band, true column widths, figures, footnotes, OMML, auto-updating TOC field, page borders — **DONE**

One-click export live-probed end-to-end: a doc with `[toc]`, display math and a
footnote exported to a 104 KB .docx whose zip contains **4 embedded `.odttf`
fonts + fontTable**, **`m:oMath` OMML**, **a real `instrText TOC` field** with
`w:updateFields` in settings.xml, and **`word/footnotes.xml` carrying the note
text**. Cover band, column widths, figures and `pgBorders`
(`docx-export.ts:1306`) ride the same shared package the Phase-1 golden tests
froze. (`lib/exports.ts` → `@docforge/export-docx` with lazily fetched font
bytes.)

## Row 9 — PDF export: pre-configured margins/headers, selectable text — **DONE**

`exportPdf` clears the preview zoom, prints, restores (`lib/exports.ts`);
margins/headers are the `@page` rules the engine emits (`themes.ts:445`) and the
`@media print` block ships only the pages (`globals.css:224`); text is real
text, so print-to-PDF output is selectable. The print dialog itself cannot be
driven headless — mechanism verified in code, identical to the classic route;
the "untick headers and footers" toast fires (probe B saw it scheduled).

## Row 10 — Markdown export; `.docforge.json` round-trip with images — **PARTIAL → DONE (2026-09-03)**

| Item | Verdict | Evidence |
| --- | --- | --- |
| Markdown export | DONE (`6af571b`) | `doExportMd` in `studio-shell.tsx` (the classic `exportMd` ported: source blob, document-title filename) with a command-palette entry. Audit-day verdict was CLASSIC-ONLY |
| Project round-trip with images | DONE | `lib/project-file.ts`: Zod v1 schema, legacy `pageBorder` migration, attachments serialized both ways; File System Access save with download fallback; `.json` routes through the project opener (`imports.ts`) |

## Row 11 — Import: docx, pdf (both roads), md, txt, html, csv/tsv, xlsx, pptx, epub, ipynb, images — drag-drop + Open + paste conversion — **PARTIAL → DONE (2026-09-03)**

| Item | Verdict | Evidence |
| --- | --- | --- |
| One import door, 12 formats | DONE | `lib/imports.ts` router; live via Open: csv, xlsx, html, ipynb, docx, and pdf-convert all replaced the source with converted Markdown (pdf strictly verified — source changed to the converted text); md/txt/tsv/pptx/epub re-driven through the real Open door 2026-09-03 (fixture files fed to the file input; each converted onto the page — md/txt verbatim, tsv as a table, pptx title as a heading, epub chapter through htmlToMd; console clean) |
| Images (drop/Open/paste) | DONE | `processImageFile` + `insertFigure`; clipboard image smart-paste wired into CodeMirror (`source-pane.tsx` paste handler) |
| Drag-drop | DONE | Full-window capture-phase DropZone naming what a drop does; routes through the same `handleImport` that Open uses (probed) |
| PDF **both roads** from the studio door | DONE | Wave one: dropping/opening a PDF raises the classic "edit in place, or convert?" dialog (`studio-shell.tsx` pdfPending); edit stashes via `stashBenchFile` and client-navigates to `/pdf`, convert takes the old road |
| Rich-HTML paste conversion | DONE | The classic structure sniff (`main.js:2292`) wired into the CM paste handler (`source-pane.tsx`) via the ported `htmlToMd`; probed live — h1/strong convert to `#`/`**`, plain text pastes untouched, one-step Ctrl+Z, classic toast |

## Row 12 — PDF bench: double-click rewrite in embedded font, fallback notice, free text, white-out, highlighter, image stamps, export from original bytes — **PARTIAL → DONE (2026-09-03)**

Live on `/pdf` (own route, no studio masthead — the §3.2 bug designed out):
opened a real PDF (4 canvases, "4 pages"), **double-click spawned an editable
`pe-edit pe-text` box carrying the original glyphs** ("C O U R S E") with the
Original-font select present, **white-out drag created a `pe-white` box**, and
**Export downloaded `assignment-edited.pdf` from the original bytes**. (Bonus:
the bench *does* have ctrl+wheel zoom, via the package.)

**Re-driven 2026-09-03** — the four remaining flows, live on `/pdf` against a
minimal Helvetica PDF: **free text** (`pe-text` box spawned and typed into),
**highlighter** (drag created a `pe-hl` band), **image stamp** (`pe-img` from
a PNG at the clicked spot), and the **font-fallback notice** ("Some characters
aren't in this PDF's embedded font…") raised on export after a double-click
rewrite of the printed line — export downloaded `bench-edited.pdf`, console
clean. Every Row-12 flow is now verified on the web surface itself.

## Row 13 — Templates ×6 working specimens; command palette; focus mode; find/replace; structure linter; autosave + crash recovery; light/dark chrome; mobile layout — **DONE**

| Item | Verdict | Evidence |
| --- | --- | --- |
| Templates | DONE | 7 specimens (welcome, assignment, proposal, report, letter, article, blank — `lib/templates.ts`), applied non-destructively with an Undo toast (`applyWithUndo`; stage-3 acceptance drove template+Undo live) |
| Command palette | DONE | Live: Ctrl+K opened it, typed "open settings" → Enter ran the command; full roster incl. templates and zoom |
| Focus mode | DONE | Live: Ctrl+Shift+Enter set `body[data-focus]`, Esc left it |
| Find/replace | DONE | Live: honest "1 of 3"/"3 matches" readout, Enter/F3 stepping, case/word/regex chips, replace field via Ctrl+H, Esc returns focus to the editor; Replace All is one CM transaction (`replaceAllMatches`) |
| Structure linter | DONE | `lib/lint.ts` + 38 unit tests (`lint.test.ts`), badge + proof-slip panel with line jumps |
| Autosave + crash recovery | DONE | Live twice: Ctrl+S then reload restored; and an *unsaved* doc (autosave only, page killed hard) was restored on the next visit (IndexedDB `restoreSession` — restore is silent, not the "Restore unsaved changes?" prompt the plan sketched, but nothing is lost) |
| Light/dark chrome | DONE | Live: Night shift/Day desk toggled `data-light` on `<html>`; documents always render on white |
| Mobile layout | DONE | Live at 780 px: one column, Source/Preview pane tabs in the ticker (`body[data-mobile-pane]` block) |

## Row 14 — Keyboard map ≥ current (Ctrl+B/I/U/F/H/S/K + new Ctrl+1/2/3, Shift+Tab) — **PARTIAL → DONE (2026-09-03)**

Every shortcut that works in the web studio, live-verified key by key (probe B):

| Keys | Does | Verified |
| --- | --- | --- |
| Ctrl+B / I / U | bold / italic / underline wrap+toggle | live |
| Ctrl+E | inline code | live |
| Ctrl+Shift+X | strikethrough | live |
| Ctrl+Shift+H | yellow highlighter | live (real-key semantics; a synthetic lowercase "h" press falls through — keyboard hardware sends "H", which binds) |
| Ctrl+1 / 2 / 3 | heading toggles (I5), re-press removes | live |
| Tab / Shift+Tab | indent / outdent | live |
| Ctrl+K (editor focused) | link wrap | live — **but see defect below** |
| Ctrl+K (elsewhere) | command palette | live |
| Ctrl+F / Ctrl+H | find / find-and-replace (replace field focused) | live |
| F3 / Shift+F3, Enter/Shift+Enter | step matches | live |
| Ctrl+S | local persist, never a download (I2 — deliberate change from classic's save-file) | live |
| Ctrl+/ | help sheet | live |
| Ctrl+Shift+Enter | focus mode | live |
| Ctrl+Z / Ctrl+Y | one history — editor and manuscript both | live |
| Esc | closes find/palette/help/drawer, then focus mode | live |

Deltas that kept this row from DONE on audit day — **all three closed since**:

- **Ctrl+P — CLASSIC-ONLY.** Classic exports PDF on it (`main.js:2321`); the web
  shell binds nothing (probed: no export fires, the browser dialog would).
- **Defect: Ctrl+K double-fires in the editor.** The CM keymap wraps the link
  *and* the shell's document-level listener opens the palette over it (probed:
  `[word](https://)` inserted **and** palette open). `lib/keymap.ts`'s own
  comment predicts exactly this collision; the shell handler never checks
  whether the editor consumed the key.
- Ctrl+wheel zoom missing (row 1).

**Closed:** Ctrl+P now runs `doExportPdf` and the shell's Ctrl+K handler carries
the same `.cm-editor` guard its F/H branch always had, so the editor keeps the
link wrap and the palette stays out of it (`e9a8310`); ctrl+wheel zoom landed in
the same commit.

---

## Summary

**Row verdicts on audit day: 8 DONE · 6 PARTIAL · 0 CLASSIC-ONLY · 0 N/A**
(14 rows). Every PARTIAL row closed within four days — see the update below.

Classic-only sub-items inside the PARTIAL rows (6): ctrl+wheel preview zoom ·
Ctrl+P export-PDF key · Markdown export · rich-HTML paste conversion · the
studio-door "edit or convert?" PDF choice · ribbon marks on a manuscript
selection. Plus one live-verified defect: the Ctrl+K double-fire. Plus four
bench flows (free text, highlighter, image stamps, fallback notice) and five
import formats (md/txt/tsv/pptx/epub) not yet re-driven on the web surfaces
they share code with.

> **Update (2026-09-03) — the audit is closed out: 14 DONE · 0 PARTIAL.**
> All six classic-only sub-items shipped — ctrl+wheel zoom and Ctrl+P
> (`e9a8310`), Markdown export and the studio-door "edit or convert?" PDF
> choice (`6af571b`), rich-HTML paste conversion (`638e098`) and ribbon marks
> on a manuscript selection (`63a9650`). The Ctrl+K double-fire is fixed. Both
> deferred re-drives are done on the real web surfaces: the five import
> formats (md/txt/tsv/pptx/epub) through the Open door, and the four bench
> flows (free text, highlighter, image stamps, font-fallback notice) on
> `/pdf`. Nothing in this appendix is outstanding.

**Shortest path to full parity, in order of value per line of code:**

1. Guard the shell's Ctrl+K handler with the same `.cm-editor` check its F/H
   branch already has (1 line) — kills the double-fire.
2. Port the classic ctrl+wheel handler onto `[data-preview-scroll]` (~8 lines
   in PreviewDeck) and bind Ctrl+P → `doExportPdf` (1 line).
3. Markdown export: `downloadBlob(new Blob([source]), name + ".md")` + a
   palette entry (~10 lines — the classic `exportMd` ports verbatim).
4. Wire `htmlToMd` (already ported and parity-harnessed) into the SourcePane
   paste handler behind the classic rich-HTML sniff (~15 lines).
5. PDF choice dialog in `handleImport` → `stashBenchFile(f)` + navigate `/pdf`
   (the stash half already exists, dead).
6. Wire `styleSelection` to the manuscript (the one real piece of work; the
   serializer is ported).
7. Re-drive the remaining bench tools and the five untested import formats on
   the web surfaces; then the §6 gate leftovers 02-studio.md already lists —
   golden corpus captured through the web render path and the Lighthouse PWA
   run (axe is recorded clean; the walkthrough below is now recorded).

---

## Keyboard-only walkthrough (recorded)

Scripted and executed live, keyboard only, fresh profile, production build
(probe C; step 10 re-verified in probe C2 after a scripting race — both restore
paths confirmed). **9/10 scripted steps passed on first run; the one scripted
failure was the probe's own timing, re-verified PASS. Recorded result: 10/10.**

| # | Step | Result |
| --- | --- | --- |
| 1 | Load `/studio`, press Tab — focus lands on the first control (the DocForge nameplate link) | PASS |
| 2 | Tab to the manuscript editor: 17 stops — nameplate · doc title · Templates · Open · Save · New · PDF · Export Word · Settings · Bench · Help · Night shift · toolbar (roving, arrows move within) · Typeface select · Size select · image tool · editor | PASS — with a note: the toolbar contributes **two** button stops, not one; `ImageTool` renders a plain `<button>` outside the Radix roving group (`image-tool.tsx:96`) |
| 3 | Write plain text into the source | PASS |
| 4 | Format via keys: Shift+←…, Ctrl+B → `**tonight.**`; Ctrl+Home, Ctrl+1 → `# My Report` | PASS |
| 5 | Ctrl+F, type "press", Enter steps to "1 of 1", Esc closes and returns focus to the editor | PASS |
| 6 | Ctrl+K, type "open settings", Enter — the settings drawer opens | PASS |
| 7 | Tab to the Theme select, ArrowDown changes it to executive (live recompose), Esc closes the drawer | PASS |
| 8 | Ctrl+K, type "export word", Enter — a real `.docx` downloads ("Welcome-to-DocForge.docx") | PASS |
| 9 | Ctrl+S — "Saved on this device" toast, no file download | PASS |
| 10 | Reload — the saved session is restored (verified for both the Ctrl+S'd doc and an autosave-only doc after a hard page kill) | PASS (on re-run with the restore given time to land; the first scripted attempt read the editor before the async restore recomposed) |

The walkthrough covers load → write → format → find → settings → export → save
→ recover without touching the pointer. The two keyboard rough edges found on
the way are recorded above: the Ctrl+K double-fire (row 14) and the ImageTool
tab stop (step 2).
